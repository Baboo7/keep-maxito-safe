import { withTranslation } from "react-i18next";

import { EnterMessage as EnterMessageComponent } from "./EnterMessage.component";

export const EnterMessage = withTranslation()(EnterMessageComponent);
